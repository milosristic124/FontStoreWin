<UserControl x:Class="UI.Controls.FamilyTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Controls"
             xmlns:Data="clr-namespace:Storage.Data;assembly=Storage"
             mc:Ignorable="d" 
             d:DesignHeight="300"
             d:DesignWidth="330">

  <ListView BorderThickness="0"
            Background="White"
            BorderBrush="{x:Null}"
            VerticalContentAlignment="Top"
            HorizontalContentAlignment="Stretch">
    <ListView.ItemTemplate>
      <DataTemplate DataType="{x:Type Data:Family}">
        <local:FamilyTreeNode />
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
  
  <!--<StackPanel x:Name="CollectionView"
              Orientation="Vertical"
              ScrollViewer.VerticalScrollBarVisibility="Auto"
              Background="White"
              Width="{Binding Width, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}"
              Height="{Binding Height, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}">
    
  </StackPanel>-->
</UserControl>
